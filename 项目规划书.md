# VBT 蜂巢 项目规划书

> 这是项目的「宪法」。忘了进度、岗位、规则，先读这个。蜂王 Q00 维护，重大变化更新。

---

## 一、项目内容

**把「震荡套利」策略吃透，用实盘数据长期验证 + 找规律。**

- 规律：冲高失败=阻力（涨到阻力做空）、探底失败=支撑（跌到支撑做多）
- 方式：实盘小额跑（每仓 10U），每个币只买一次，攒样本找规律
- **铁律：实战优先**。回测结论不成立，只有实盘数据才是依据。报盈亏算净值（已实现 + 浮盈），不能只算平仓那部分。

## 二、岗位分配（定死了，不重复）

### 脚本（1 个，24 小时跑）

| 脚本 | 工作 |
|---|---|
| `backtest/live_scan.py` | 实盘自动下单 + 补仓，每币只买一次 |

### Agent（5 个，各司其职）

| Agent | 岗位 | 工作内容 | 记录文件夹 |
|---|---|---|---|
| **watcher** | 盯盘监控 | 实时盯仓盈亏、报警（浮亏<-2.5U 或浮盈>+4.5U）、扫新币信号 | `agents/watcher/` |
| **script-keeper** | 脚本维护 | 修 bug、维护 4 个脚本、保证数据准确 | `agents/script-keeper/` |
| **pattern-finder** | 规律研究 | 分析平仓数据找规律（多空/时间/币种），不武断贴标签 | `agents/pattern-finder/` |
| **observer** | 每日复盘 | 每天写一份独立笔记（每日观察-日期.md），长期积累 | `agents/observer/` |
| **optimizer** | 参数分析 | 用实盘数据分析参数，回测仅参考非定论 | `agents/optimizer/` |

## 三、实盘参数（当前）

| 参数 | 值 |
|---|---|
| 每仓保证金 | 10U |
| 杠杆 | 10x |
| 每仓名义 | 100U |
| 止盈 | 保证金 50%（= 价格 +5%） |
| 止损 | 保证金 30%（= 价格 -3%） |
| 每币 | 只买一次，平仓不再开 |
| 过滤 | 价格 < 0.0001 的币跳过 |

## 四、关键数据文件

| 文件 | 内容 | 更新频率 |
|---|---|---|
| `backtest/output/level_coins.json` | 阻力/支撑（信号基准） | 每 6 小时 |
| `backtest/output/trade_log.json` | 平仓记录 | 每 30 分钟 |
| `backtest/output/traded_coins.json` | 已买过的币 | 实时（脚本） |
| `backtest/output/coin_library.json` | 币库（455币含杠杆） | 每天 08:57 |

## 五、子 Agent 记录规范

- 每个 Agent 往自己的 `agents/<name>/` 文件夹记录（独立文件，不互相覆盖）
- 重要的事 SendMessage 报蜂王；日常记录自己写文件夹
- observer 每天一份独立笔记，长期积累成档案

## 六、定时任务

| 任务 | 频率 |
|---|---|
| 平仓记录 | 每 30 分钟 |
| 实盘健康检查 | 每 30 分钟 |
| 阻力/支撑刷新 | 每 6 小时 |
| 币库更新 | 每天 08:57 |
| 盯盘深度检查 | 每小时 |
| 每日复盘（observer） | 每天 00:03 |

## 七、踩过的坑（重要）

- 张数必须查 ctVal：`qty = int(NOTIONAL/(ct_val*last))`，漏算下错 10 倍
- 下单失败不能标记「已买」，否则烧候选池
- 余额用 availEq（可用保证金），不是 eq（总权益）
- trade_recorder 方向用 positions-history 的 direction 字段（net 模式 posSide 恒 "net"）
- trade_recorder 逐条腿记录，唯一键 posId|ctime（不能按 posId 聚合，会漏腿）
- OKX 抓数据必须带 User-Agent，否则 403
- WS login 时间戳用秒（10位），不是毫秒

## 八、GitHub 同步

- `okx-wallet-H/bee-strategy` → 信号对接（给 H 机器人下单）
- `okx-wallet-H/vbt-research` → 研究资料（Agent Teams 子文件夹 + 本规划书）
- 本地工作区 `_research_repo/`，蜂王定期 push
