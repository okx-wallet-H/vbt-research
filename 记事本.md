# 记事本 — VBT 蜂巢工作记录

> 蜂王 Q00 每次有重大决策/状态变化就更新这里。重启后先读这个接上进度。

---

## 一、实盘（当前在跑）

- **状态**：10 仓满仓自动跑，走总部 exec 接口 `http://154.64.254.42:3001`（token `bee-exec-2024-secret`）
- **每仓**：10U 保证金、10x 杠杆、100U 名义
- **止盈**：保证金 50%（= 价格 +5%）；**止损**：保证金 30%（= 价格 -3%）
- **下单**：市价单（总部默认），OCO 自动挂止盈止损
- **脚本**：`backtest/live_scan.py --live`（后台跑，每 5 秒扫 47 候选币，贴阻力做空/贴支撑做多，扫满余额约 10 仓）
- **过滤**：价格 < 0.0001 的币跳过（PEPE 精度丢，裸奔过）

## 二、关键决策（用户拍板的）

1. 止盈止损按**保证金比例**算：止盈 50%、止损 30%（不是价格百分比，也不是锚定阻力支撑位）
2. 杠杆 **10x**，每仓保证金 **10U**，扫完账户余额
3. 市价单先用，**限价单后面再优化**
4. 研究类工作**全权自主**（派 Agent/研究/GitHub 同步不用问），实盘大决策仍报用户

## 三、研究结论（已吃透，别重复劳动）

- **震荡套利 K线阻力支撑 = 证伪**（8 币胜率 27~37% 负 EV，参数过拟合 OOS 掷硬币）→ `策略研究/50`、`参数优化/55`
- **官方 API 能替代总部 exec**（下单 5 种+算法单、止盈止损灵活、批量 20 单）→ `API调研/60`
- **订单簿挂单墙真实存在，但和 K线位差 10 倍尺度**（分层用：K线做日级、订单簿做日内）→ `订单簿/61`
- **账号 VIP3**，`books-l2-tbt`（400档逐笔）拿不到（要 VIP4），公开 `books`（400档快照）够用
- 唯一结构性 edge：**SOL/DOGE 下行做空**（趋势属性）

## 四、Agent Teams 分工

| Agent | 负责 | 子文件夹（vbt-research 仓库） |
|---|---|---|
| watcher | 实盘盯盘 | 盯盘/ |
| pattern-finder | 策略研究/找规律 | 策略研究/ |
| optimizer | 参数优化/回测 | 参数优化/ |
| api-researcher | 官方 API 调研 | API调研/ |
| orderbook-researcher | 订单簿/挂单墙 | 订单簿/ |
| live_scan | 实盘自动下单 | 实盘/ |

## 五、仓库

- `okx-wallet-H/bee-strategy` —— 信号对接（给 H 机器人下单，signals/strategy.py）
- `okx-wallet-H/vbt-research` —— 研究资料（Agent Teams 子文件夹，40 份报告）
- 本地工作区：`_signal_repo/`、`_research_repo/`

## 六、币库

- `backtest/output/coin_library.json` —— 全市场 455 个 USDT 永续（含最大杠杆/面值/现价/成交额）
- 每日更新脚本 `backtest/update_coin_library.py`，cron 每天 08:57（**session 级，重启要重挂**）
- 杠杆分布：100x(5: BTC/ETH/SOL/XRP/XAU)、50x(80)、20x(269)、10x(76)

## 七、待办 / 进行中

- [进行中] **订单簿事件研究**（orderbook-event Agent，跑 45~60 分钟，统计碰墙反弹/穿透命中率，~13:25 出结果）
- [待调研] **全市场 275+ 扫 + 批量下单 + 账户级 5% 止盈**（用户想法，等这轮实盘跑完看质量再定）
- [待做] **杠杆倍数测试**（币库已标最大杠杆，分档测 5x/10x/20x/50x 对盈亏和爆仓距离的影响）
- [待做] 限价单优化（总部 exec 加限价，或切官方 API）
- [待提] 总部 exec 接口让 H 优化（加限价单、改止盈止损、批量下单）

## 八、踩过的坑

- OKX WS login 时间戳用**秒**（10位），不是 REST 的毫秒（13位），否则报 60006
- 合约面值 `ctVal` 各币不同（DOT=1、GMT=1、MMT=10、XRP=100），下单张数 = 名义 ÷ (ctVal×现价)，漏算会下错 10 倍
- PEPE 等超小币（<0.0001）止盈止损价格 round 6 位精度丢，OCO 设置失败裸奔
- 总部 exec 的 `closeOrderAlgo` 字段不返回止盈止损，查 OCO 要调 OKX `orders-algo-pending?ordType=oco`
